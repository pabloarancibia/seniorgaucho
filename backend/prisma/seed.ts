/**
 * El contenido de las lecciones (MDX) lo provee el usuario. Este script
 * queda listo para poblarlas contra POST /api/lessons cuando estén
 * disponibles; por ahora no siembra nada.
 */
async function main(): Promise<void> {
  console.log("No hay lecciones para sembrar todavía.");
}

main();
